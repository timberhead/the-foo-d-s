<section class="card">
  <a href="/dish/{{id}}">
    <header>{{name}} Rated: {{stars}}/5</header>
    <img src="/img/{{image_file}}" alt="{{description}}" />
  </a>
  <p>Description: {{description}}</p>
</section>